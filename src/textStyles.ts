import { StyleSheet } from "react-native";

export const TEXT_STYLES = StyleSheet.create({
    title: {
        fontSize: 14,
        lineHeight: 17,
        fontWeight: '500',
        color: 'white',
        marginTop: 8,
    }
});